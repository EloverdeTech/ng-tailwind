import { NgtStylizableDirective } from './ngt-stylizable.directive';

describe('NgtStylizableDirective', () => {
  it('should create an instance', () => {
    const directive = new NgtStylizableDirective();
    expect(directive).toBeTruthy();
  });
});
